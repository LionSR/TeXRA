# Build output
dist/
out/
releases/

# Dependencies
node_modules/
pnpm-lock.yaml

# CLI validation-only bundle and copied resources
packages/cli/.texra-validate-run/

# Trace-viewer bundles copied into the extension at build time (git-ignored)
packages/extension/resources/traceViewer/
packages/extension/resources/traceViewerShared/

# YAML templates with {{ }} placeholders - Prettier rewrites these as { { } }
packages/extension/resources/templates/

# Claude Code agent scratch space (worktrees contain unstaged template files)
.claude/

# Local git worktrees (contain unstaged template files prettier cannot parse)
.worktrees/

# GitHub Actions workflows — the format-pr workflow's GITHUB_TOKEN lacks
# `workflows: write` permission, so any auto-formatting of workflow YAML
# triggers a "refusing to update workflow" push rejection. Skip them
# entirely from format runs.
.github/workflows/

# Retired sources kept for restoration (see attic/README.md) - Deno-formatted
attic/
