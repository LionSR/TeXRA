/** Shared labels for approving delegated work within one run. */
export const DELEGATION_APPROVAL_COPY = {
  cliAction: 'approve this and all later tasks, edits & commands in this run',
  cliCompactAction: 'later tasks + actions in run',
  progressViewAction:
    'Approve this and all later tasks, edits & commands in this run',
} as const;
