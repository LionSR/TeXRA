# Valid guidance

Read the [agent guide](agent/core/README.md) and the root
[contributor guidance](../AGENTS.md).

<!-- prettier-ignore -->
Read the [balanced draft](guide_(draft).md) and the
[titled draft](guide_(draft).md (Draft guide)).

Plain prose foo](nowhere.md) is not a Markdown link.

A backticked filename whose stem is a run-vocabulary namespace is a path, not a
citation: `run.ts`, `context.ts`, `flow.md`, `output.xml`, `output.tex` and
`output.bib` are files, while `flow.step` and `tool.result` are rows the
vocabulary declares.
