/**
 * `noopTexraTrace` is a deprecated alias for {@link noopTrace}.
 * The TexraTrace sugar methods collapsed into helpers, so the no-op
 * variants no longer need their own implementation.
 */
export { noopTrace as noopTexraTrace } from '@agent/trace';
