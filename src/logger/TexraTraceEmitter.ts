/**
 * `TexraTraceEmitter` is a deprecated alias for {@link TraceEmitter}.
 * See `TexraTrace.ts` for context — the host-extension class collapsed
 * into helpers in `@agent/trace`. New code should construct a
 * `TraceEmitter` directly.
 */
export { TraceEmitter as TexraTraceEmitter } from '@agent/trace';
