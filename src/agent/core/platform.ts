/**
 * Platform re-exports for backward compatibility.
 *
 * New code should import directly from `@platform/platform`.
 */
export { initPlatform, platform, tryPlatform, type Platform } from '@platform/platform';
