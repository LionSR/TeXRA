/**
 * Identifier Types and Execution Model
 *
 * RE-EXPORT from @shared/identifiers for backward compatibility.
 * New code should import directly from '@shared/identifiers'.
 *
 * @deprecated Import from '@shared/identifiers' instead
 */
export {
  StreamTabIdSchema,
  ExecutionIdSchema,
  StorageKeySchema,
  ExecutionIdentitySchema,
  type StreamTabId,
  type ExecutionId,
  type StorageKey,
  type ExecutionIdentity,
} from '@shared/identifiers';
