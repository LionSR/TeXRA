// RE-EXPORTED MODULE
// -------------------
// `workspaceFileUtils` has been merged into `fsUtils`.
// This stub keeps backward compatibility; new code should import from `fsUtils`.

export * from './fsUtils';