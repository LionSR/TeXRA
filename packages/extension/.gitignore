# Copied from the repo root by scripts/copy-extension-docs.mjs during
# `pnpm run build:fast` so vsce includes them in the VSIX. The source of
# truth lives at the repo root.
README.md
CHANGELOG.md

# Copied from node_modules by scripts/copy-html-export-assets.mjs during
# `pnpm run package:fast` so the HTML chat export ships with KaTeX +
# highlight.js styling.
resources/htmlExport/
