# Copied from the repo root by scripts/copy-extension-docs.mjs during
# `pnpm run build:fast` so vsce includes them in the VSIX. The source of
# truth lives at the repo root.
README.md
CHANGELOG.md

# Built by packages/trace-viewer's vite configs during `pnpm run package:fast`
# (prepare:trace-viewer-assets) — the multi-file bundle (shared-assets export
# mode, served over http/https) and the single-file, fully self-contained
# bundle (default export mode, opens via file:// with no server) used by the
# `--export html` CLI export and the Settings view's "Export as HTML" button.
resources/traceViewer/
resources/traceViewerStandalone/

# Copied from the repo root skills/ tree by scripts/copy-extension-skills.mjs
# during `pnpm run package`/`build:fast` so vsce includes the bundled skill
# catalog in the VSIX. The source of truth lives at the repo root; dev/debug
# builds don't need this present since src/skills/skillSources.ts falls back
# to the repo-root skills/ tree when this directory is missing.
resources/skills/
