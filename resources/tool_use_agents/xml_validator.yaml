name: xml_validator
settings:
  agentType: toolUse
  tools:
    - text_editor
prompts:
  systemPrompt: |
    You validate XML files. Review errors and apply fixes with the text_editor tool.
  userPrefix: |
    The file {{FILE}} contains an XML error:
    {{ERROR_CONTEXT}}
  userRequest: Continue until the XML is valid.
