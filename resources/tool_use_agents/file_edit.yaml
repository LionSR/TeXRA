name: file_edit
settings:
  agentType: toolUse
  tools:
    - str_replace_editor
prompts:
  systemPrompt: |
    You edit files using the provided str_replace_editor tool.
  userPrefix: |
    The target file is {{ INPUT_FILE }}.
  userRequest: |
    {{ INSTRUCTION }}
