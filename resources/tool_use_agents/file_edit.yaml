name: file_edit
settings:
  agentType: toolUse
  tools:
    - text_editor
prompts:
  systemPrompt: |
    You edit files using the provided text_editor tool.
  userPrefix: |
    The target file is {{FILE}}.
  userRequest: |
    {{INSTRUCTION}}
