{
  "semantics": "Public (static + instance) method declarations of StreamLogStore and StreamSnapshotStore, per issue #9590 Stage 5 (proof obligation 10). Every public method is a caller-verified contract, and every field exposed by a configured aggregate snapshot getter is one caller-visible contract unit. Replacing five field getters with getRunMetadata therefore leaves the StreamSnapshotStore contract budget unchanged: the aggregate method contributes five units, not one. Transcript row mutation is writer-only (TranscriptWriter via acquireWriter/loadAndAcquireWriter), snapshot event projection enters only through attachSessionEvents, and 'protected' counts as public because neither class is subclassed. The ratchet rejects growth in either public methods or configured aggregate fields. When the surface genuinely shrinks, lower this baseline in the same PR. requestEviction (2026-09-02) is the one sanctioned addition: the sidecar counterpart of StreamLogStore.requestEviction, requested by the session lifecycle for finished child streams so a long session's record set stops growing; see .agents/docs/archived/simplification/2026-09-02-stream-lifetime-and-cancellation-simplification.md section 2.C. StreamSnapshotStore.deleteStream was removed on the same day (#11769): it was a zero-caller wrapper over stageDeleteStream + commit, so the surface returns to 21. S1 read-time phase derivation (2026-09-04): StreamLogStore is net zero. hasUnfinishedOutput replaces getUnfinishedStreamIds: the store answers one stream at a time, and SessionState.resolveStreamPhase asks it per displayed stream instead of scanning (the boot-time callers were deleted with restart repair). StreamSnapshotStore is net zero too, and stays at 25: the run tuple the phase rule needs (outcome, checkpointPresent, lease, authorityFailure) is not the store's. It is read at ROW OPEN by SessionState.hydrateRunFacts, held in that session's own in-memory map, and never persisted, so getRunPhaseFacts and its four aggregate fields are gone again and the store keeps only what a record owns. Summary-meta mirror retired (2026-09-07): StreamLogStore.getSummaryMeta and recordSummaryMeta had no production caller left once the snapshot store stopped publishing display metadata into the summary, so both methods, the persisted `meta` block and its schema are deleted and StreamLogStore drops from 22 to 20. Baseline audit on the same day: StreamSnapshotStore listed five names the class no longer declares (flush, listStagedDeletions, reconcileStagedDeletions, stageDeleteStream, workPlanProvenance), all removed by 3d8b07e628 when the fold took over snapshot reads and deletion commits; the ratchet only rejects growth, so those stale entries were silently granting five units of headroom. They are removed here and StreamSnapshotStore drops from 21 to 16 (20 contract units with getRunMetadata's five fields). Cutover reconciliation (2026-09-07): the event-backed transcript conversion also removed endRunningGroupsForStreams, flush, getTimestampRange, hasUnfinishedOutput, keys and openReadOnlyForStream. Its committed-event input is acceptCommitted. Recording that replacement reduces StreamLogStore from 20 listed methods to its actual 15, with no unused contract headroom.",
  "stores": {
    "StreamLogStore": [
      "acceptCommitted",
      "acquireWriter",
      "clear",
      "delete",
      "ensureLoaded",
      "ensureStream",
      "ephemeral",
      "get",
      "has",
      "hasAuthoritativeStream",
      "loadAndAcquireWriter",
      "onChange",
      "open",
      "readEntries",
      "requestEviction"
    ],
    "StreamSnapshotStore": [
      "attachSessionEvents",
      "getCompileFailures",
      "getExecutionIdMap",
      "getKnownFilePaths",
      "getMissingOutputs",
      "getOutputFiles",
      "getParentStreamId",
      "getRunMetadata",
      "getRunUsage",
      "getWorkPlan",
      "hasProvenance",
      "listPersistedStreams",
      "load",
      "preload",
      "read",
      "requestEviction"
    ]
  },
  "aggregateContracts": {
    "StreamSnapshotStore": {
      "getRunMetadata": [
        "config",
        "description",
        "executionId",
        "identity",
        "userFollowUpSupport"
      ]
    }
  }
}
