import{at as e,it as t,st as n,yt as r}from"./chunks/framework.B-d2E8F-.js";import{t as i}from"./chunks/CliMultiAgentHero.BCk17efd.js";var a=JSON.parse(`{"title":"Multi-agent workflows","description":"","frontmatter":{},"headers":[],"relativePath":"guide/multi-agent-workflows.md","filePath":"guide/multi-agent-workflows.md"}`),o=Object.assign({name:`guide/multi-agent-workflows.md`},{setup(a){return(a,o)=>(r(),t(`div`,null,[o[0]||=e(`<h1 id="multi-agent-workflows" tabindex="-1">Multi-agent workflows <a class="header-anchor" href="#multi-agent-workflows" aria-label="Permalink to “Multi-agent workflows”">​</a></h1><p>Some problems are too big for one agent: checking every derivation in a long calculation, attacking a conjecture from several directions at once, or fixing, verifying, and merging a set of files. A multi-agent workflow is how a team lead attacks that kind of problem. Instead of delegating one task at a time and waiting, the lead writes a short script that says which specialists to run, which of them can run in parallel, and how their results feed the next step. TeXRA then runs that script for it: fan-out, joins, and the final synthesis happen in plain code, with no model round-trip between steps, and the run can pick up where it left off if it is interrupted.</p><p>The tool behind this is called <code>delegate_multi_agents</code>. You never write the script yourself. The orchestrator writes it, you review the plan, and the run shows up like any other delegation, one row per agent.</p><h2 id="when-the-lead-reaches-for-it" tabindex="-1">When the lead reaches for it <a class="header-anchor" href="#when-the-lead-reaches-for-it" aria-label="Permalink to “When the lead reaches for it”">​</a></h2><p>Team leads such as <code>orchestrator</code> and <code>engineer</code> have three ways to hand work off. <code>delegate_workflow</code> and <code>delegate_agent</code> run one agent at a time, which is right when the next decision depends on reading the previous result. <code>delegate_multi_agents</code> is for the other case: the complete shape of the work is known before anything runs.</p><p>Typical shapes:</p><ul><li><strong>Fan out, then merge.</strong> Fix or audit several files in parallel, then pass the corrected files to one merge step.</li><li><strong>Pipeline.</strong> Draft, then review, then apply the review, where each stage waits for the previous one.</li><li><strong>Survey and decide.</strong> Run several specialist analyses that each return a small structured answer, then let the script pick the best one or hand the set to a synthesis agent.</li></ul><p>The lead still plans in conversation with you first. It reads the project, proposes an interpretation, and only then writes the script.</p><h2 id="what-a-run-looks-like" tabindex="-1">What a run looks like <a class="header-anchor" href="#what-a-run-looks-like" aria-label="Permalink to “What a run looks like”">​</a></h2><ol><li><strong>A proposal you approve.</strong> Before anything runs, the lead&#39;s script is shown as a proposal labelled <strong>Multi-agent workflow</strong> with the script&#39;s name, its phases, any declared items, the default agent and model, and the files available to the script. Declared items are plan labels from the script, not resolved calls: a script whose calls depend on runtime data declares none, and the card says so instead of counting zero. The defaults are just that — each call may name its own agent and model. The card also carries a cost warning, because calls run concurrently. You can approve, or reject with feedback so the lead adjusts its plan. The script itself is saved under <code>.texra/workflow-scripts/</code> in your workspace, so you can open it.</li><li><strong>Phases and per-call progress.</strong> The run opens as its own stream. Calls are grouped under the phases the script declared. A declared item appears as a quiet <strong>Declared</strong> row once its phase opens, until the script issues it; from then on the row is a real call and shows what it is — <strong>Document</strong> (a workflow agent editing files) or <strong>Structured</strong> (a tool-use agent returning validated data), the agent and model it runs, and the files it was handed — along with its status: Planned (issued but not yet queued), Queued (waiting for a concurrency slot), Running, Finished, Saved result (replayed from an earlier attempt), Skipped, Cancelled, or Failed. Rows running at the same time are the run&#39;s real concurrency; sharing a phase does not by itself mean calls run together or depend on each other. When a call finishes, its row adds the elapsed time and what it cost.</li><li><strong>Skip or retry a running call.</strong> In the CLI, focus a running task in the subagent panel and press <code>s</code> to skip it or <code>r</code> to retry it; <code>k</code> kills it. A skipped call is excluded from the synthesis step by the script.</li><li><strong>A summary when it finishes.</strong> The lead receives the script&#39;s return value, the run log, and a one-line summary: phases run, tasks succeeded out of total, total cost, duration, and every file the run produced with its diff counts. Workflow-agent outputs land in run storage like any other delegated run; the lead reviews them and uses <code>accept_run_files</code> to bring them into the workspace.</li></ol>`,10),n(i),o[1]||=e(`<p class="hero-caption">A team session in the CLI. A multi-agent workflow&#39;s calls appear in this same subagent panel, grouped by phase, each as a focusable stream with its own transcript.</p><p>The run is detached: the lead gets an execution id back immediately and keeps working while the workflow runs. It can check on progress with the <code>executions</code> tool, and the result is delivered to it as a follow-up message when the run completes.</p><h2 id="a-minimal-script" tabindex="-1">A minimal script <a class="header-anchor" href="#a-minimal-script" aria-label="Permalink to “A minimal script”">​</a></h2><p>This is the shape of script the lead writes. It fixes two drafts in parallel, then merges the corrected files in a second phase.</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> meta</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fix-drafts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  description: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix typos in two drafts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  phases: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Merge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tasks: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;first&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix first draft&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, phase: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;second&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix second draft&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, phase: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;merge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Merge corrected drafts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, phase: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Merge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">phase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> results</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> parallel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  files.inputFiles.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slice</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">index</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fix spelling errors only.&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        id: index </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;first&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;second&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        inputFiles: [file],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> correctedFiles</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> results</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;__WORKFLOW_SKIPPED__&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">flatMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">result</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result.outputs.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">output</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> output.absolutePath));</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">phase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Merge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Merge the corrected drafts.&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;merge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  inputFiles: correctedFiles,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>A few things to notice. The <code>meta</code> block is the plan: because <code>tasks</code> is declared, the proposal and the progress view can show all three tasks before any of them run. <code>parallel()</code> runs the two fixes concurrently and waits for both. A failed call resolves to <code>null</code> and a call you skipped resolves to <code>&#39;__WORKFLOW_SKIPPED__&#39;</code>, so the script filters both out before the merge. Each workflow-agent call resolves to a result that lists the files it produced, and those files can be handed straight to the next call.</p><h2 id="the-script-api" tabindex="-1">The script API <a class="header-anchor" href="#the-script-api" aria-label="Permalink to “The script API”">​</a></h2><p>Scripts run in a sandbox with no imports and no access to the filesystem, network, or clock. Only these primitives exist:</p><table tabindex="0"><thead><tr><th style="text-align:left;">Primitive</th><th style="text-align:left;">What it does</th></tr></thead><tbody><tr><td style="text-align:left;"><code>export const meta</code></td><td style="text-align:left;">Required first statement. <code>name</code> and <code>description</code> are required. <code>phases</code> lists phase titles, <code>tasks</code> is the optional declared plan (<code>{ id, label, phase? }</code>), and <code>timeoutMs</code> sets the whole-run wall clock (1 second to 60 minutes; default 10 minutes).</td></tr><tr><td style="text-align:left;"><code>agent(prompt, options?)</code></td><td style="text-align:left;">Runs one specialist and returns a promise. A workflow-agent call takes <code>inputFiles</code> (editable), <code>contextFiles</code>, and <code>mediaFiles</code> (read-only) and resolves to a result listing its output files, diffs, and cost. A call with <code>schema</code> (a JSON Schema object) runs a tool-use agent named by <code>agentName</code> and resolves to a result whose <code>.structured</code> field is the validated object. <code>model</code> picks a specific model for the call.</td></tr><tr><td style="text-align:left;"><code>parallel(thunks)</code></td><td style="text-align:left;">Runs an array of zero-argument functions concurrently and waits for all of them. Failed calls resolve to <code>null</code>; a thrown script error fails the whole run.</td></tr><tr><td style="text-align:left;"><code>phase(title)</code></td><td style="text-align:left;">Marks the start of a phase for progress display. Titles must be declared in <code>meta.phases</code>.</td></tr><tr><td style="text-align:left;"><code>log(message)</code></td><td style="text-align:left;">Writes a line to the run log that the lead receives with the result.</td></tr><tr><td style="text-align:left;"><code>args</code></td><td style="text-align:left;">The JSON value the lead passed when launching the script, for data-dependent task sets.</td></tr><tr><td style="text-align:left;"><code>files</code></td><td style="text-align:left;">The files bound to the run, as <code>files.inputFiles</code>, <code>files.contextFiles</code>, and <code>files.mediaFiles</code>.</td></tr></tbody></table><p>Ordinary JavaScript handles the rest: a <code>for</code> loop with awaited calls is a pipeline, and array methods such as <code>.filter()</code> and <code>.map()</code> are how results fan back in. Up to four agent calls run at once across the whole script, a single run makes at most 200 live calls, and <code>Date.now()</code> and <code>Math.random()</code> are unavailable so that a rerun replays the same call sequence.</p><h2 id="checkpoints-and-resume" tabindex="-1">Checkpoints and resume <a class="header-anchor" href="#checkpoints-and-resume" aria-label="Permalink to “Checkpoints and resume”">​</a></h2><p>Every completed <code>agent()</code> call is written to a journal before its result is handed back to the script. The journal is keyed by the script&#39;s <code>meta.name</code> together with the default agent and the lead&#39;s session, so resume works within the same session rather than across sessions. Each entry records the call&#39;s prompt and options, plus the bytes of any files it read.</p><p>If the run times out, is interrupted, or fails partway through, nothing that finished is lost. The lead calls the tool again in the same session with the same <code>meta.name</code>, and calls whose prompt, options, and input files are unchanged replay from the journal at no cost, showing as <strong>Saved result</strong> in the progress view. Only the calls that did not finish, or that the lead changed in the script, run again. A new <code>meta.name</code> starts over from scratch.</p><p>This also means the lead can edit a script after a failure rather than rewriting it. Every result points at the saved script file, and the tool accepts a path to that file instead of new source.</p><p>In the CLI, a workflow run resumes headless: <code>texra resume &lt;id&gt;</code> continues a stored workflow execution under its original id and honors <code>--print</code>, <code>--output-format</code>, and <code>--no-input</code>. The lead&#39;s own session is a tool-use session, so resuming it reopens the interactive chat, where you can ask it to rerun the script. Read <a href="./texra-cli.html#execution-history">Execution history in the CLI guide</a> for the commands.</p><h2 id="where-it-is-available" tabindex="-1">Where it is available <a class="header-anchor" href="#where-it-is-available" aria-label="Permalink to “Where it is available”">​</a></h2><p><strong>Agents.</strong> The tool is only offered to agents whose configuration names it. Today that is the <code>orchestrator</code> lead (the Physicist, Mathematician, and Computer Scientist teams), <code>leanOrchestrator</code> (the Lean Project team), and the <code>engineer</code> lead (the Software Engineer team). <code>orchestrator</code> and <code>leanOrchestrator</code> are <a href="./remote-agents.html">remote agents</a>, so they need a TeXRA sign-in; <code>engineer</code> is built in. A <a href="./custom-agents.html">custom agent</a> can list <code>delegate_multi_agents</code> in its tools too.</p><p><strong>The global switch.</strong> Naming the tool is only half of the opt-in. The <strong>Multi-Agent Workflow</strong> switch on the <strong>Tools</strong> tab of the Dashboard is a kill switch on top of it: when it is off, the tool is removed from every agent&#39;s tool list, whatever the agent&#39;s configuration says. New installs start with it off, so turn it on before asking a lead to use it. From the CLI, the same switch is <code>texra tools enable workflow-script</code>.</p><p><strong>Hosts.</strong> The VS Code extension and the desktop app show the proposal, the phase and task progress, and the delivery summary in the ProgressBoard. The CLI shows the same run in the chat TUI&#39;s subagent panel, with the skip and retry keys above. In a headless <code>texra run</code>, no proposal prompt can be shown, so the workflow proceeds without one; the approval policy you pass still governs what its child agents may edit or execute.</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to “Troubleshooting”">​</a></h2><p><strong>The lead never offers a multi-agent workflow.</strong> Check the <strong>Multi-Agent Workflow</strong> switch on the Dashboard&#39;s <strong>Tools</strong> tab (or <code>texra tools status workflow-script</code>). When it is off the tool is stripped from every agent, and the lead falls back to delegating one task at a time. Also confirm the lead is one of the agents listed above.</p><p><strong>&quot;A workflow script run for meta.name ... is already in progress&quot;.</strong> The lead tried to launch a script whose previous run is still running or finishing. Only one run per name is allowed at a time, so a second launch is refused rather than starting a competing run over the same journal. Wait for the first run to deliver, then resume with the same name if it did not complete.</p><p><strong>The run stopped with a wall-clock timeout.</strong> The default limit for a whole run is 10 minutes, which a large fan-out on a slow model can exceed. Completed calls are journaled, so ask the lead to rerun the script with the same <code>meta.name</code>; it can raise <code>meta.timeoutMs</code> in the saved script (up to 60 minutes) for the second attempt.</p><p><strong>&quot;Workflow agent ... edits files: pass options.inputFiles&quot;.</strong> A workflow-agent call was made without input files. Workflow agents rewrite documents, so each call needs <code>inputFiles</code>, or a previous call&#39;s outputs, unless the agent declares default output files. Analysis that returns a value rather than a file belongs in a tool-use call with <code>schema</code>.</p><p><strong>A task shows Failed while the rest continue.</strong> That is the intended behavior: a failed <code>agent()</code> call resolves to <code>null</code> and the script carries on with the calls that succeeded. Open the task&#39;s stream to read why it failed. Failed calls are not journaled, so a rerun retries them.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to “Next steps”">​</a></h2><ul><li><a href="./built-in-agents.html#built-in-teams">Built-in agents</a>: the teams whose leads can run workflows</li><li><a href="./texra-cli.html#multi-agent-teams">TeXRA CLI</a>: running a team from the terminal</li><li><a href="./agent-architecture.html">Workflow agents</a>: what a single workflow-agent call does</li><li><a href="./agent-integrations.html">Agent integrations</a>: the Tools tab and approval settings</li><li><a href="./custom-agents.html">Custom agents</a>: give your own lead agent the tool</li></ul>`,27)]))}});export{a as __pageData,o as default};