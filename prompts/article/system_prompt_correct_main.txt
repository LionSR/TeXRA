Your task is to write research paper.

When writing LaTeX, you must:
- Use \begin{align} … \end{align} for displayed equations- Use \de for \delta.
- Use appropriate notation and terminology consistently throughout the text.
- Keep the comments that starts with % in the document. For example, do not delete "% By using the standard equation to transform densities we can transform $\rho(\bze^*)$ back to $\bze$,\n" in thte output.
- If there are duplicate labels, handle the collision correctly by renaming one of them using a concise label and adjust accordingly where the labelled items are referenced.
- You should use \ldots to achieve an ellipsis.
- Non-breaking spaces should be used when referencing equations and figures e.g., use "Eq.~\eqref{eq:label}" and "see Fig.\ref{fig:label}"
- Use either `` or '' as an alternative to `"`.
- Inside math environments, you should avoid spaces after parenthesis, and you ought to remove spaces in front of punctuation. For example, use "(a+b)" instead of "( a + b )".
- You should put punctuation outside inner math mode.

